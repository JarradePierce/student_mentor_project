  <ul>
    <% mentor.availabilities.each do |availability| %>
      <li>Time: <%= availability.start_time.in_time_zone("Pacific Time (US & Canada)").strftime("%l:%M%P %A, %B %-m, %Y") %> PST</li>
    <% end %>
  </ul>